pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello World"'

                    echo "Hwello again"
                    ls -lah
            }
        }
    }
}